# Browser-facing configuration. Leave VITE_API_URL empty in production when the
# frontend and API are served from the same origin through the reverse proxy.
VITE_API_URL=http://localhost:8080
VITE_TMDB_API_KEY=
